# ============================================================
# ⚡ GRADLE PERFORMANCE & CACHING (Optimized for Gradle 9.x)
#File: gradle.properties
# ============================================================
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.caching=true
# Gradle 9.x configuration cache is stable — enable for full speed true / false , must be set to false when making jPackageImage
org.gradle.configuration-cache=false
org.gradle.configuration-cache.parallel=false
org.gradle.unsafe.isolated-projects=false
# Use isolated projects (safe + faster on 9.x)
# Auto-adjust worker threads to CPU count (safe upper limit 8)
org.gradle.workers.max=8
# ============================================================
# 🧠 MEMORY & JVM SETTINGS
# ============================================================
# Balanced memory: 4 GB heap + tuned metaspace + UTF-8 encoding
org.gradle.jvmargs=-Xms1g -Xmx4g -XX:MaxMetaspaceSize=512m -XX:+UseG1GC -XX:+ParallelRefProcEnabled -XX:MaxGCPauseMillis=50 -XX:+UnlockExperimentalVMOptions -XX:+UseStringDeduplication -Dfile.encoding=UTF-8
# ============================================================
# 🧩 LOGGING & CONSOLE
# ============================================================
# quiet / warn / lifecycle / info / debug
org.gradle.logging.level=quiet
# none / summary / fail / all
org.gradle.warning.mode=all
org.gradle.console=rich
org.gradle.welcome=never
# Disable unnecessary reports
createReports=false
# ============================================================
# 💾 FILESYSTEM / WINDOWS SPECIFICS
# ============================================================
# ReFS (Dev Drive) has known issues with native VFS watching
org.gradle.vfs.watch=true
org.gradle.vfs.verbose=false
org.gradle.daemon.idletimeout=600000
# ============================================================
# ☕ JDK TOOLCHAIN CONFIGURATION
# ============================================================
org.gradle.java.installations.auto-detect=true
org.gradle.java.installations.auto-download=false
org.gradle.java.installations.fromEnv=JAVA_HOME
org.gradle.java.installations.paths=I:/14_JDKs/graalVM/graalvm-jdk-25_windows-x64_bin/graalvm-jdk-25.0.1+8.1
org.gradle.java.home=I:/14_JDKs/graalVM/graalvm-jdk-25_windows-x64_bin/graalvm-jdk-25.0.1+8.1
# ============================================================
# ⚙️ RAM DRIVE CACHE (Superfast)
# ============================================================
# R:\ = 1GB SoftPerfect RAMDisk, NTFS, used for Gradle cache
org.gradle.cache.dir=R:/PosMan/.gradlePosManCache
org.gradle.build-cache-directory=R:/PosMan/.gradle-build-cache
# ============================================================
# 🧩 MISC CUSTOM
# ============================================================
debug=false
kotlin.internal.collectFUSMetrics=false
kotlin.build.report.output=none
